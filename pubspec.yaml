name: repaso_todos_theme
description: A new Flutter project.

publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ">=2.17.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons:
  flutter_bloc:
  equatable: 

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
